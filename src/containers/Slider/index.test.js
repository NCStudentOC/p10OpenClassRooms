import { render, screen } from "@testing-library/react";
import Slider from "./index";
import { api, DataProvider } from "../../contexts/DataContext";

const data = {
  focus: [
    {
      title: "World economic forum",
      description:
        "Oeuvre à la coopération entre le secteur public et le privé.",
      date: "2022-02-29T20:28:45.744Z",
      cover: "/images/evangeline-shaw-nwLTVwb7DbU-unsplash1.png",
    },
    {
      title: "World Gaming Day",
      description: "Evenement mondial autour du gaming",
      date: "2022-03-29T20:28:45.744Z",
      cover: "/images/evangeline-shaw-nwLTVwb7DbU-unsplash1.png",
    },
    {
      title: "World Farming Day",
      description: "Evenement mondial autour de la ferme",
      date: "2022-01-29T20:28:45.744Z",
      cover: "/images/evangeline-shaw-nwLTVwb7DbU-unsplash1.png",
    },
  ],
};

describe("When slider is created", () => {
  it("a list card is displayed", async () => {
    window.console.error = jest.fn();
    api.loadData = jest.fn().mockReturnValue(data);
    render(
      <DataProvider>
        <Slider />
      </DataProvider>
    );
    await screen.findByText("World economic forum");
    await screen.findByText("janvier");
    await screen.findByText(
      "Oeuvre à la coopération entre le secteur public et le privé."
    );
  });
});


describe("When a page is created", () => {
  it("a Slider is displayed", () => {
    render(<Slider />);
    const slider = screen.findByTestId("testSlider");
    expect(slider).toBeDefined();
  })

  it("a SlideCardList is displayed", () => {
    render(<Slider />);
    const SlideCardList = screen.findByTestId("testSlideCardList");
    expect(SlideCardList).toBeDefined();

  })


  it("SlideCard__pagination is displayed", () => {
    render(<Slider />);
    const SlideCard__pagination = screen.findByTestId("testSlideCard__pagination");
    expect(SlideCard__pagination).toBeDefined();
  })



});